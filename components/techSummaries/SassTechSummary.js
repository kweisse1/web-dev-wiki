import React from "react"
import {Paragraph} from "../../components/"

function SassTechSummary(props)
{
return(

<>
 <Paragraph text={`             Sass is a CSS preprocessor. It complies to CSS which can be parsed by the browser. Sass provides developers with powerful tools to make writing CSS more pleasant, such as; variables, nested rules, mixins and functions`}/>
</>

)

}
export default SassTechSummary